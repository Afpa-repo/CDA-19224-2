{{ form_start(form) }}
<div class="row">
    <div class="col-md-2">{{ form_row(form.price) }}</div>
    <div class="col-md-2">{{ form_row(form.soldout) }}</div>
    <div class="col-md-2">{{ form_row(form.promo) }}</div>
</div>
{{ form_widget(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer')}}</button>
{{ form_end(form) }}


